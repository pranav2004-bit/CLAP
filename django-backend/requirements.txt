# Django Backend for CLAP - Requirements
# Python 3.10+

# Core Django
Django==4.2.9
djangorestframework==3.14.0
django-cors-headers==4.3.1
python-decouple==3.8

# Database
psycopg2-binary==2.9.9

# Authentication & Security
bcrypt==4.1.2
PyJWT==2.9.0
djangorestframework-simplejwt==5.3.1
# cryptography is required for Phase 2.3 CloudFront signed URL generation (RSA-SHA1).
# It is also used by PyJWT for RS256/ES256 JWT algorithms.
# bcrypt>=4.0 no longer depends on it, so we pin it explicitly.
cryptography==42.0.8

# OpenAI Integration
openai==1.10.0
# openai==1.10.0 is incompatible with httpx>=0.28 (proxies arg removed)
httpx==0.27.2
# tiktoken: token counter for pre-flight OpenAI quota estimation (gpt-4o-mini = cl100k_base).
# Soft dependency — token_counter.py falls back to char-ratio if not installed.
tiktoken==0.7.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# Development
django-extensions==3.2.3
ipython==8.20.0

# Production
gunicorn==21.2.0
whitenoise==6.6.0

# Async Processing & Infra
celery==5.4.0
redis==5.0.8
boto3==1.35.24
django-storages==1.14.4

# PDF generation (report worker)
# pydyf 0.12.x has an incompatible API change (stream.transform signature)
# that breaks WeasyPrint 62.3. Pin to 0.11.0 until WeasyPrint releases a fix.
weasyprint==62.3
pydyf==0.11.0

# Audio conversion (WebM/MP4 → MP3 64kbps mono, runs in celery-reports worker)
# Requires ffmpeg system binary — installed in Dockerfile runtime stage.
pydub==0.25.1

# Error tracking / observability (D4/E5)
# Set SENTRY_DSN in .env to activate; safe no-op if DSN is absent.
sentry-sdk[django,celery]==2.19.2
